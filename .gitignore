# MCP credentials (never commit)
.mcp.json

# env files
.env
.env.local
.env.*.local
.env.development
.env.production

# Next.js
.next/
out/
build/

# Node
node_modules/

# OS
.DS_Store

# Vercel
.vercel

# TypeScript
tsconfig.tsbuildinfo

# Local salon scrape / PDF exports (not for git)
generate_salons_pdf.py
sofia_salons_no_website.pdf
plovdiv_salons.pdf
*_salons*.pdf
*.mp4
.env*

# Sibling client repos that may be cloned inside this folder for convenience
# during development. They have their own GitHub repos and must never be
# committed here.
salon-paradise-repo/
salon-paradise/

# Local AI assistant config (skill definitions, agent settings, Claude CLI
# settings). Not project code — belongs to the developer's machine.
.agents/
.claude/
